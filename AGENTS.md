# 本项目的协作与说明要求

- 编写文档、代码注释、操作说明和答复时，交代必要背景、前提与具体步骤；这是持续适用的要求。仓库文档不评价成员的经验或技术水平。
- 解释优先用中文。保留准确的英文文件名、代码标识符和命令，首次出现时说明中文含义，避免用英文类名代替职责说明。
- 操作指引写清前提、所在目录、具体步骤、预期结果和常见失败处理；计划中的命令与已验证可执行命令要区分。
- 用具体业务例子解释概念，例如用"办卡和收款一起成功或回滚"解释事务。方法说明交代输入、输出、异常和数据变更。
- 写业务代码之前，先在设计中确定输入、返回类型、可空字段与 SQL 格式。业务列表统一 Page[T]，详情统一 View，失败抛异常；禁止同一接口随分支在字典、列表、None 之间切换。变更接口时同步调用方和相关测试。
- 设计集中维护在 `docs/architecture.md`，参与步骤在 `docs/README.md`，规范在 `docs/project-standards.md`。优先更新现有文档，避免重复建一套说明。
- 当前架构已于 2026-09-19 获项目发起人确认，后续按已确认范围推进，不重复请求同一项架构批准。确认设计不等于已完成实现，状态以实际文件和验证结果为准。
- 开发时应同步记录到 `docs/dev-materials-for-report/` 下的相关文档：开发日志 (`development-log.md`) 记录进度与问题解决过程，设计决策 (`design-decisions.md`) 记录架构选型与权衡，测试笔记 (`testing-notes.md`) 记录测试发现与修复，保证报告材料的真实性。
- 默认 CLI，`--tui` 启用可选 TUI；两者共用业务服务，CLI 不依赖 TUI 完成或安装其可选库。
- 文档只写"做什么"，不写"不做什么"或"为什么不做某事"。被要求删除内容时，直接删除即可，不要留下排除理由、替代方案说明或设计权衡解释。保持文档简洁，聚焦实际采用的方案。

---

## 协作技能库

**代码审计** → 查看 [.agents/skills/code-review/SKILL.md](.agents/skills/code-review/SKILL.md)  
在提交代码或 PR 前进行全方位审计（业务逻辑、安全性、质量、性能、测试、文档同步）
